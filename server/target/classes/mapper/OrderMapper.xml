<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.an.mapper.OrderMapper">

    <insert id="insert" parameterType="com.an.entity.Order" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders(order_number, user_id, total_amount, status, shipping_address, shipping_phone, created_at, updated_at)
        VALUES (#{orderNumber}, #{userId}, #{totalAmount}, #{status}, #{shippingAddress}, #{shippingPhone}, #{createdAt}, #{updatedAt});
    </insert>

    <select id="getById" resultType="com.an.entity.Order">
        SELECT * FROM orders WHERE id = #{id}
    </select>

    <select id="listByUserId" resultType="com.an.entity.Order">
        SELECT * FROM orders
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <update id="updateStatus">
        UPDATE orders SET
                          status = #{status},
                          updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updatePayTime">
        UPDATE orders SET
                          payment_time = #{payTime},
                          updated_at = NOW()
        WHERE id = #{id}
    </update>
    <update id="update" parameterType="com.an.entity.Order">
        UPDATE orders
        SET status = #{status},
            payment_time = #{paymentTime},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 销售端分页查询 -->
    <select id="adminList" resultType="com.an.entity.Order">
        SELECT *
        FROM orders
        ORDER BY created_at DESC
    </select>

    <!-- 逻辑删除（你没有is_deleted字段也没关系，按你需要改） -->
    <update id="deleteById">
        UPDATE orders SET status = -1 WHERE id = #{id}
    </update>

    <!-- 日销售额统计 -->
    <select id="dailySales" resultType="com.an.vo.DailySalesVO">
        SELECT
            DATE(payment_time) AS date,
            SUM(total_amount) AS totalAmount
        FROM orders
        WHERE (status = 'paid' OR status = 'delivered')
          AND payment_time BETWEEN #{begin} AND #{end}
        GROUP BY DATE(payment_time)
        ORDER BY DATE(payment_time)
    </select>

    <!-- 销售概览统计 -->
    <select id="summary" resultType="com.an.vo.SalesSummaryVO">
        SELECT
            SUM(total_amount) AS totalSales,
            COUNT(*) AS totalOrders,
            AVG(total_amount) AS avgOrderAmount
        FROM orders
        WHERE (status = 'paid' OR status = 'delivered')
          AND payment_time BETWEEN #{begin} AND #{end}
    </select>

    <select id="getFullById" resultMap="orderWithItemsMap">
        SELECT *
        FROM orders
        WHERE id = #{id}
    </select>

    <!-- 订单 + 订单项 resultMap -->
    <resultMap id="orderWithItemsMap" type="com.an.entity.Order">
        <id property="id" column="id"/>

        <result property="orderNumber" column="order_number"/>
        <result property="userId" column="user_id"/>
        <result property="status" column="status"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="shippingAddress" column="shipping_address"/>
        <result property="shippingPhone" column="shipping_phone"/>

        <result property="paymentTime" column="payment_time"/>
        <result property="shippingTime" column="shipping_time"/>
        <result property="deliveryTime" column="delivery_time"/>

        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>

        <!-- 关联订单项 -->
        <collection property="items" ofType="com.an.entity.OrderItem"
                    select="com.an.mapper.OrderItemMapper.listByOrderId"
                    column="id" />
    </resultMap>

</mapper>
